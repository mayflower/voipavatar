# Service dependencies
livekit>=0.11.0
livekit-api>=0.6.0
livekit-agents>=0.8.0
numpy>=1.24.0
opencv-python>=4.8.0
soundfile>=0.12.0
pyyaml>=6.0

# MuseTalk dependencies (some overlap with MuseTalk's own requirements)
torch>=2.0.0
torchaudio>=2.0.0
diffusers>=0.24.0
transformers>=4.35.0
accelerate>=0.25.0

# MMLab stack (for face detection/pose estimation)
# These are installed separately in Dockerfile due to specific build requirements
# mmengine
# mmcv
# mmdet
# mmpose
